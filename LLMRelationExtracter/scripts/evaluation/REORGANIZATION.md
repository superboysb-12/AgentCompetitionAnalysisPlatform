# 评估脚本重组完成

## ✅ 已完成的更改

### 目录结构

```
LLMRelationExtracter/
├── scripts/
│   └── evaluation/                    # 新建：评估脚本目录
│       ├── model_evaluator.py         # 核心评估器
│       ├── run_evaluation.py          # 运行脚本
│       ├── test_evaluation.py         # 测试脚本
│       ├── requirements_evaluation.txt # 依赖列表
│       ├── README.md                  # 快速指南
│       └── EVALUATION_GUIDE.md        # 完整文档
│
└── data/
    └── output/
        └── evaluation_results/        # 新建：评估结果输出目录
            ├── model_comparison_*.xlsx
            ├── model_comparison_*.md
            ├── model_comparison_*.json
            └── *.png (图表)
```

## 🚀 使用方法

### 1. 安装依赖

```bash
cd LLMRelationExtracter
pip install -r scripts/evaluation/requirements_evaluation.txt
```

### 2. 运行自检

```bash
python scripts/evaluation/test_evaluation.py
```

自检内容:
- ✅ 依赖包是否安装
- ✅ 知识图谱文件是否存在
- ✅ 评估器模块是否正常

### 3. 运行评估

```bash
python scripts/evaluation/run_evaluation.py
```

评估结果将保存到 `data/output/evaluation_results/`

## 📊 输出文件

运行后会在 `data/output/evaluation_results/` 生成:

1. **model_comparison_YYYYMMDD_HHMMSS.xlsx**
   - 综合对比
   - 质量指标
   - Schema符合度
   - 性能与成本
   - 详细指标

2. **model_comparison_YYYYMMDD_HHMMSS.md**
   - 综合排名
   - 各维度最佳
   - 详细分析

3. **可视化图表**
   - radar_comparison_*.png (雷达图)
   - bar_comparison_*.png (柱状图)
   - scatter_perf_quality_*.png (散点图)
   - confidence_dist_*.png (分布图)

4. **model_comparison_YYYYMMDD_HHMMSS.json**
   - 完整评估数据

## 🎯 评估维度

| 维度 | 权重 | 主要指标 |
|------|------|----------|
| 质量评分 | 30% | 平均置信度、高/低置信度比例、稳定性 |
| Schema符合度 | 20% | 配置内比例、关系/实体符合度 |
| 一致性 | 15% | 实体命名一致性、关系分布均匀度 |
| 多样性 | 15% | 类型数量、信息熵 |
| Evidence质量 | 10% | spans覆盖率、长度合理性 |
| 成本效益 | 10% | Token效率、三元组/Token比 |
| 速度 | - | 处理时间、吞吐量 |
| **综合得分** | - | **0-100分** |

## 🔧 配置要评估的模型

编辑 `scripts/evaluation/run_evaluation.py`:

```python
models_to_evaluate = {
    "deepseek-v3": "data/output/knowledge_graph_deepseek.json",
    "gemini-2.5-flash": "data/output/knowledge_graph_gemini-2.5-flash.json",
    "gpt-5": "data/output/knowledge_graph_gpt-5.json",
}
```

## 📖 详细文档

- **快速指南**: `scripts/evaluation/README.md`
- **完整文档**: `scripts/evaluation/EVALUATION_GUIDE.md` (180+行)
  - 评估指标详解
  - 评分算法说明
  - 报告解读指南
  - 自定义配置
  - 常见问题FAQ
  - 进阶用法

## 💡 关键特性

✅ **完全自动化** - 无需人工标注，节省时间
✅ **8维度评估** - 全面对比模型性能
✅ **可视化报告** - 图表直观易懂
✅ **独立目录** - 脚本和结果分离
✅ **路径智能** - 自动处理相对路径
✅ **可重复性** - 标准化评估流程

## 🎨 路径更新说明

所有脚本已更新为使用正确的相对路径:

1. **model_evaluator.py** - 无需修改，使用传入的路径
2. **run_evaluation.py** - 自动定位到项目根目录
3. **test_evaluation.py** - 自动定位到项目根目录
4. **输出目录** - 统一到 `data/output/evaluation_results/`

## ⚠️ 注意事项

1. **运行位置**: 在 `LLMRelationExtracter` 目录下运行所有脚本
2. **输入文件**: 确保已经运行 `main.py` 生成知识图谱
3. **依赖安装**: 首次使用需要安装评估依赖包
4. **Python版本**: 需要 Python 3.8+

## 🆚 对比旧版

| 项目 | 旧版 | 新版 |
|------|------|------|
| 脚本位置 | 根目录 | scripts/evaluation/ |
| 结果位置 | evaluation_results/ | data/output/evaluation_results/ |
| 路径管理 | 相对路径 | 自动处理 |
| 组织性 | 分散 | 集中管理 |

---

**重组完成时间**: 2025-10-10
**版本**: v1.0
