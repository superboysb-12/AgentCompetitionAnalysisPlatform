# Agent Competition Analysis Platform - Dependencies
#
# 安装说明:
# 请根据需要的模块安装对应的依赖
#
# ============================================
# 方式1: 完整安装（所有模块）
# ============================================
# pip install -r requirements.txt
#
# ============================================
# 方式2: 按模块安装
# ============================================
# Backend模块（FastAPI服务）:
#   cd backend && pip install -r requirements.txt
#
# Smart Crawler模块:
#   cd crawl && pip install -r requirements.txt
#
# LLM Relation Extracter模块:
#   cd LLMRelationExtracter && pip install -r requirements.txt
#
# Relation Extracter模块（可选，PaddleNLP方案）:
#   cd RelationExtracter && pip install -r requirements.txt


# ============================================
# 核心依赖（所有模块共用）
# ============================================
pyyaml>=6.0
tqdm>=4.65.0
pandas>=2.0.0
requests>=2.32.0
python-dotenv>=1.0.0


# ============================================
# Backend模块依赖（FastAPI服务）
# ============================================
# Web框架
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 数据库
sqlalchemy>=2.0.25
pymysql>=1.1.0
cryptography>=41.0.0
alembic>=1.13.0

# 任务队列
celery>=5.3.0
redis>=5.0.0

# 调度器
apscheduler>=3.10.0

# 图数据库
neo4j>=5.14.0

# 工具
loguru>=0.7.0
httpx>=0.26.0


# ============================================
# Smart Crawler模块依赖
# ============================================
playwright>=1.40.0


# ============================================
# LLM Relation Extracter模块依赖
# ============================================
openai>=1.0.0
zhipuai>=2.0.0
tiktoken>=0.5.0
numpy>=1.24.0
scikit-learn>=1.3.0
rapidfuzz>=3.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
openpyxl>=3.1.0


# ============================================
# Relation Extracter模块依赖（可选，如使用PaddleNLP方案）
# ============================================
# PaddlePaddle (GPU版本 - 推荐)
# 安装命令: pip install paddlepaddle-gpu==3.1.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
# paddlepaddle-gpu==3.1.0

# PaddlePaddle (CPU版本 - 备选)
# 安装命令: pip install paddlepaddle==3.1.0
# paddlepaddle==3.1.0

# paddlenlp>=3.0.0b4
# torch>=2.0.0
# transformers>=4.30.0
# spacy>=3.5.0
# fastcoref>=2.1.0


# ============================================
# Text Classification模块依赖（GUI标注工具，可选）
# ============================================
# flet>=0.21.0


# ============================================
# 开发依赖（可选）
# ============================================
# pytest>=7.4.0
# pytest-asyncio>=0.23.0
# black>=23.0.0
# flake8>=6.0.0
# mypy>=1.0.0


# ============================================
# 注意事项
# ============================================
# 1. Backend模块需要MySQL和Redis服务，可选Neo4j图数据库
# 2. PaddlePaddle GPU版本需要CUDA支持，请根据你的CUDA版本选择对应的PaddlePaddle版本
# 3. Playwright首次使用需要安装浏览器: playwright install chromium
# 4. 某些包可能需要系统级依赖，请参考各包的官方文档
# 5. 建议使用虚拟环境管理依赖: python -m venv venv
# 6. Neo4j数据库可选安装，仅在需要知识图谱存储时使用
